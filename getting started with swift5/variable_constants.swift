// Concept 1 : Variables and Constants

var currentHealth = 90
let maxHealth = 100

currentHealth = 100 + 1 

// We could not able to change value of maxHealth as it is declared as constant wih `let`
// maxHealth = 100 + 1 ;

print(currentHealth)
print(maxHealth)
